<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <changeSet id="create-payments-table" author="oms-dev" context="base">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="payments"/>
            </not>
        </preConditions>
        <createTable tableName="payments">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true"/>
            </column>
            <column name="order_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="monto" type="DECIMAL(12,2)">
                <constraints nullable="false"/>
            </column>
            <column name="metodo_pago" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="estado_pago" type="VARCHAR(20)" defaultValue="PENDIENTE">
                <constraints nullable="false"/>
            </column>
            <column name="referencia_externa" type="VARCHAR(255)"/>
            <column name="fecha_pago" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <changeSet id="add-payments-foreign-key" author="oms-dev" context="base">
        <addForeignKeyConstraint
                baseTableName="payments"
                baseColumnNames="order_id"
                referencedTableName="orders"
                referencedColumnNames="id"
                constraintName="fk_payments_order"
                onDelete="RESTRICT"
                onUpdate="CASCADE"/>
    </changeSet>

    <changeSet id="add-payments-indexes" author="oms-dev" context="base">
        <createIndex tableName="payments" indexName="idx_payments_estado">
            <column name="estado_pago"/>
        </createIndex>
    </changeSet>

    <changeSet id="add-payments-unique-constraint" author="oms-dev" context="base">
        <addUniqueConstraint tableName="payments" columnNames="order_id" constraintName="uk_payments_order"/>
    </changeSet>

    <changeSet id="add-payments-constraints" author="oms-dev" context="base">
        <sql>
            ALTER TABLE payments
            ADD CONSTRAINT ck_payments_monto CHECK (monto > 0)
        </sql>
        <sql>
            ALTER TABLE payments
            ADD CONSTRAINT ck_payments_metodo_pago
            CHECK (metodo_pago IN ('TARJETA_CREDITO', 'TARJETA_DEBITO', 'TRANSFERENCIA', 'EFECTIVO'))
        </sql>
        <sql>
            ALTER TABLE payments
            ADD CONSTRAINT ck_payments_estado_pago
            CHECK (estado_pago IN ('PENDIENTE', 'COMPLETADO', 'FALLIDO', 'REEMBOLSADO'))
        </sql>
    </changeSet>

</databaseChangeLog>
